const readingsDatabase: { [key: string]: { measure_type: string, measure_datetime: Date, customer_code: string, image_url: string, confirmed_value: boolean, measure_value: number } } = {};

export default readingsDatabase
